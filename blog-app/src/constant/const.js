const rootUrl = "https://api.realworld.io/api/";
const baseURL = "http://localhost:8080";

const articleUrl = rootUrl + "articles";
const tagsUrl = rootUrl + "tags";

const signupURL = baseURL + "/user-service/signup";
const loginURL = baseURL + "/user-service/login";

export { rootUrl, articleUrl, tagsUrl, signupURL, loginURL };
